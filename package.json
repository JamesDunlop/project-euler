{
  "name": "project-euler",
  "private": true,
  "version": "0.0.0",
  "description": "A set of solutions to Project Euler by James Dunlop",
  "repository": "https://github.com/JamesDunlop/project-euler",
  "license": "MIT",
  "devDependencies": {
    "bower": "^1.3.1",
    "http-server": "^0.8.0"
  },
  "scripts": {
    "postinstall": "bower install",

    "prestart": "npm install",
    "start": "http-server -a localhost -p 8000 -c-1",

    "pretest": "npm install",
    "test": "karma start karma.conf.js",
    "test-single-run": "karma start karma.conf.js  --single-run"
  }
}
